package Lab6;

public enum ClientCommand {
    remove_all,
    remove,
    show,
    add_if_max,
    info,
    insert,
    remove_lower,
    exit,
    help, //think about not sending help and exit
    other,
    get_map
}
