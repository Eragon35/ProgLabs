package Lab6;

public enum ClientCommand {
     remove_all,
     remove,
     show,
     add_if_max,
     info,
     insert,
     remove_lower,
     exit,
     help,
     other,
     get_map,

//     sign_in,
     add_user;
}
